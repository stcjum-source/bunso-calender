# 분소 업무 달력 — 웹앱 설정 가이드

폰·아무 PC에서 **로그인해서 나만** 쓰고, 고치면 모든 기기에 **자동 동기화**됩니다.
순서: ① Supabase 준비(로그인·저장소) → ② config.js에 값 2개 넣기 → ③ GitHub에 올리기 → ④ 폰 홈화면에 추가.
소요 15~20분, 전부 무료 범위.

---

## ① Supabase 준비 (로그인 + 데이터 저장소)

### 1-1. 프로젝트 만들기
1. https://supabase.com 접속 → 로그인(계정 있으면 그대로).
2. **New project** → 이름(예: `bunso-calendar`), DB 비밀번호 아무거나 정하고 **Create**.
3. 1~2분 기다리면 생성 완료.

### 1-2. 데이터 표 만들기 (복붙 한 번)
왼쪽 메뉴 **SQL Editor** → **New query** → 아래를 통째로 붙여넣고 **Run**:

```sql
create table if not exists calendar_state (
  user_id uuid primary key references auth.users(id) on delete cascade,
  data jsonb not null,
  updated_at timestamptz not null default now()
);
alter table calendar_state enable row level security;

create policy "own select" on calendar_state for select using (auth.uid() = user_id);
create policy "own insert" on calendar_state for insert with check (auth.uid() = user_id);
create policy "own update" on calendar_state for update using (auth.uid() = user_id);
```

> 이 설정(RLS) 덕분에 **로그인한 본인 데이터만** 보이고 고칠 수 있습니다. 남은 못 봅니다.

### 1-3. 내 로그인 계정 만들기 (나만 쓰게)
1. 왼쪽 **Authentication** → **Users** → **Add user** → **Create new user**.
2. 내 이메일 + 비밀번호 입력, **Auto Confirm User** 체크 → **Create**.
   - 이게 앱에서 로그인할 아이디/비번입니다.
3. **공개 가입 차단**(남이 회원가입 못 하게): **Authentication → Sign In / Providers → Email** 에서 **Allow new users to sign up**(또는 *Enable signups*)을 **끄기(OFF)**.

### 1-4. 값 2개 복사
왼쪽 **Project Settings(톱니) → API** 에서:
- **Project URL** (예: `https://abcd1234.supabase.co`)
- **anon public** 키 (`eyJ...`로 시작하는 긴 문자열)

> anon 키는 원래 **공개용**이라 노출돼도 안전합니다. 실제 잠금은 위 로그인+RLS가 합니다.

---

## ② config.js에 값 넣기

앱 폴더의 **config.js** 파일을 메모장으로 열어 따옴표 안에 붙여넣기:

```js
window.BUNSO_CONFIG = {
  SUPABASE_URL: "https://abcd1234.supabase.co",   // ← 1-4의 Project URL
  SUPABASE_ANON_KEY: "eyJhbGciOiJI...",           // ← 1-4의 anon public 키
  AUTO_LOCK_MINUTES: 15                            // 15분 무조작 시 자동 잠금(0=끄기)
};
```

저장.

> 값을 비워두면 로그인 없이 "이 기기에만 저장"되는 미리보기로 동작합니다. 동기화하려면 반드시 채우세요.

---

## ③ GitHub에 올려서 주소 만들기 (GitHub Pages, 무료·HTTPS)

폰에서 앱처럼 쓰려면 **https 주소**가 필요합니다(서비스워커·홈화면 추가 조건).

1. GitHub에서 **New repository** → 이름(예: `bunso-cal`) → **Public** → **Create**.
   - Public이어도 데이터는 로그인 뒤에만 보이니 안전합니다.
2. **uploading an existing file** 클릭 → 이 폴더 안 **모든 파일**을 드래그해서 올리고 **Commit**.
   (index.html, app.mjs, supabase.js, config.js, 아이콘 등 전부. `설정가이드.md`는 안 올려도 됨)
3. **Settings → Pages** → *Source*를 **Deploy from a branch**, 브랜치 **main / (root)** → **Save**.
4. 1~2분 뒤 상단에 주소가 뜸: `https://<내아이디>.github.io/bunso-cal/`
   → 이 주소가 앱 주소입니다.

> config.js를 나중에 고치면 그 파일만 다시 올리면(덮어쓰기) 반영됩니다.

---

## ④ 폰 홈화면에 앱으로 추가

위 주소를 폰 브라우저로 열고:

- **아이폰(사파리)**: 공유 버튼 → **홈 화면에 추가**.
- **안드로이드(크롬)**: 우측 ⋮ → **홈 화면에 추가 / 앱 설치**.

이제 홈 아이콘을 누르면 전체화면 앱처럼 열립니다. 처음 한 번 **로그인**(1-3의 이메일/비번)하면 유지됩니다.

---

## 사용 방법 · 프라이버시

- **분소 PC**: 브라우저로 주소 열기 → 로그인 → 사용 → 끝나면 우측 상단 **🔒 잠금** 누르기(또는 15분 뒤 자동 잠금). 그러면 다음 사람은 로그인 화면만 봅니다.
  - 더 확실히 하려면 **시크릿(비공개) 창**으로 열기.
- **폰·집 PC**: 로그인 상태 유지되어 바로 사용.
- 한 곳에서 고치면 다른 기기에서 앱을 열거나 화면을 다시 볼 때 자동 반영됩니다.

## 알아둘 점 (솔직히)

- 로그인 화면으로 가리는 건 **옆사람·동료가 열어봐도 못 보게** 하는 수준입니다. 쓰는 도중 어깨너머로 보는 것까지는 못 막습니다.
- 여러 기기에서 **동시에** 같은 항목을 고치면 마지막 저장이 이깁니다(단독 사용엔 문제 없음).
- 인터넷이 끊긴 동안 고친 내용은 이 기기에 임시 보관됐다가, 연결되면 자동으로 다시 저장 시도합니다.

## 다음 단계 (원하면)

지금 데스크톱 위젯(띄워놓기·크기조절)도 이 로그인 방식으로 연결할 수 있습니다. 웹앱이 폰에서 잘 되는 걸 확인한 뒤 진행하세요.
